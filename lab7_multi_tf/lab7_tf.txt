LAB — Two-LLM Terraform Pipeline
LLM 1 Builds Terraform, LLM 2 Reviews It

🎯 Lab Goal

By the end of this lab, students will:
    use one LLM to generate Terraform
    use a second LLM to review Terraform
    see how multi-step AI pipelines improve trust
    understand the beginnings of multi-agentic IaC workflows

🧱 Architecture
LLM 1 — Builder
The Builder creates Terraform based on requirements.

Example tasks:
    VPC
    public/private subnets
    S3 bucket
    EC2 instance
    security groups

LLM 2 — Reviewer
The Reviewer checks for:
    missing resources
    naming issues
    missing tags
    security problems
    incomplete outputs
    poor practices

The second LLM should not just say “looks good.”
It should be trained to think like a strict reviewer.

🏗 Suggested First Scenario

For the first lab, keep the infrastructure scope simple and recognizable.

Scenario: Create Terraform for:
    1 AWS VPC
    2 public subnets
    1 internet gateway
    1 route table
    route table associations
    useful tags

This is easy enough to follow, but rich enough for review.

🗂 Project Setup
Create a new folder:

    mkdir -p tf_two_llm
    cd tf_two_llm
    touch tf_two_llm.py

🐍 Full Python Script

Paste this into tf_two_llm.py

https://github.com/BalericaAI/Ai_Class/blob/main/lab7_multi_tf/python/tf_two_llm.py

Terraform code to review:
            {terraform_code}

            """.strip()

def extract_improved_terraform(review_text: str) -> str:
marker = "IMPROVED_TERRAFORM:"
start = review_text.find(marker)
if start == -1:
return ""

        hcl_start = review_text.find("```hcl", start)
        if hcl_start == -1:
            return ""
        
        hcl_start += len("```hcl")
        hcl_end = review_text.find("```", hcl_start)
        if hcl_end == -1:
            return ""
        
        return review_text[hcl_start:hcl_end].strip()

def save_file(filename: str, content: str):
with open(filename, "w", encoding="utf-8") as f:
f.write(content)

def main():
print("\n☁️ Two-LLM Terraform Pipeline\n")


